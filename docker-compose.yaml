services:
  resizer:
    container_name: resizer
    build: .
    volumes:
      - ./:/app
    working_dir: /app

  resizer-test-nginx:
    image: nginx:latest
    ports:
      - "${NGINX_PORT}:80"
    volumes:
      - ./:/var/www